name: arm-read
on:
  push:
    branches:
      - 'dependabot/npm_and_yarn/read-**'
permissions:
  contents: read
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: npm ci
      - name: Show probe result
        if: always()
        run: cat PROBE_RESULT.txt || echo "no result file"
